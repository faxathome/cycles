<div id="flash">
  <% flash.each do |key, value| %>
    <div class="flash alert <%= alert_class(key) %> fade show"><%= value %>
      <button class="close" data-dismiss="alert">×</button>
    </div>
  <% end %>
</div>
